switch 怎么定义类型:
1. 一个类型定义一个非终结符的一种形态
2. 一个类型定义一个非终结符的所有形态
3. 一个类型定义所有非终结符的所有形态 √

switch 怎么使用标记:
1. 严格对应程序文法结构，标记1表示非终结符的类型，标记2表示非终结符形态的类型
2. 严格对应程序文法结构，标记表示所有终结符的所有形态
3. 不严格对应程序文法结构，在2的基础上，合并相同行为的标记，去除没有行为的标记 √

switch 怎么表示子树:
1. 一个节点成员对应一棵子树
2. 一个节点数组成员对应所有子树 √